% EXAMPLE: Custom Fail Function
% This shows how to handle exceptions with your own logic
baseDir = fileparts(mfilename('fullpath'));
dataDir = fullfile(baseDir, 'data');

tableOfFiles = table(["data1"; "data2" ; "dataIsNotThere";  "dataIsCorrupted"] , 'VariableNames', {'FileNames' });



function fName = getCompleteFileName(f)

end